# Optional. If unset, Vite derives owner/repo from `git remote get-url origin`
# (repository root = parent of `web/`). CI sets this from github.repository.
# VITE_GITHUB_REPO=your-login/jre-vidget
# VITE_APP_TITLE=My vidget
